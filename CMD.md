## Commands

```bash
dotnet run --project src/AssetDownloader/AssetDownloader.fsproj bcircle/capture-service@latest --token $GITHUB_TOKEN  --output temp

wk-asset-downloader bcircle/capture-service@latest --token $GITHUB_TOKEN  --output temp
```